#  we have no missing data so we are not doing any thing if we had than we would aee that how much data is missed if it is above than 10% than we would remove those rows but if it is more than 10% than we would fill that mean if the data is missing more than 10% than
# Mean imputation,for normal distribution
# df['column'].fillna(df['column'].mean(), inplace=True)

# # Median imputation, for skewed distribution
# df['column'].fillna(df['column'].median(), inplace=True)

# # Mode imputation, for categorical columns
# df['column'].fillna(df['column'].mode()[0], inplace=True)
